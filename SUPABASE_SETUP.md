# Налаштування Supabase для синхронізації даних

Ця інструкція допоможе вам налаштувати Supabase для синхронізації даних між мобільною та веб-версією додатку.

## Крок 1: Створення акаунту Supabase

1. Перейдіть на https://supabase.com
2. Зареєструйтеся або увійдіть у свій акаунт
3. Створіть новий проект:
   - Натисніть **"New Project"**
   - Введіть назву проекту (наприклад, "FilmHub")
   - Введіть пароль для бази даних (збережіть його!)
   - Виберіть регіон (найближчий до вас)
   - Натисніть **"Create new project"**
   - Зачекайте 1-2 хвилини, поки проект ініціалізується

## Крок 2: Отримання API ключів

1. Після створення проекту в лівому меню натисніть **"Settings"** (⚙️)
2. Виберіть **"API"** в меню Settings
3. Скопіюйте наступні значення:
   - **Project URL** (це буде `SUPABASE_URL`) - знаходиться в розділі "Project URL"
   - **Publishable API Key** (це буде `SUPABASE_PUBLIC_KEY`) - знаходиться в розділі "Project API keys" → "Publishable"

⚠️ **ВАЖЛИВО:** Використовуйте ТІЛЬКИ **Publishable** ключ, НЕ використовуйте **Secret** ключ у клієнтському коді!

## Крок 3: Налаштування файлу конфігурації

1. Відкрийте файл `FilmHub/lib/supabase.js`
2. Замініть значення на ваші реальні:

```javascript
const SUPABASE_URL = 'https://ваш-проект.supabase.co';
const SUPABASE_PUBLIC_KEY = 'ваш-publishable-ключ';
```

Приклад:
```javascript
const SUPABASE_URL = 'https://wacxpbswdkpaqvjnzuyl.supabase.co';
const SUPABASE_PUBLIC_KEY = 'sb_publishable_lnI3XaHrFal7C-0O_wJkvQ_OfVvYYDY';
```

## Крок 4: Створення таблиць у Supabase

### Де писати SQL код:

1. У вашому проекті Supabase в лівому меню натисніть **"SQL Editor"** (📝)
2. Натисніть кнопку **"New query"** (або "+" вгорі)
3. Скопіюйте та вставте весь SQL код знизу в редактор
4. Натисніть кнопку **"Run"** (або натисніть Ctrl+Enter / Cmd+Enter)

### SQL код для виконання:

```sql
-- Створення таблиці users для зберігання додаткової інформації про користувачів
CREATE TABLE IF NOT EXISTS public.users (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT UNIQUE NOT NULL,
  role TEXT NOT NULL DEFAULT 'user',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Створення таблиці movies
CREATE TABLE IF NOT EXISTS public.movies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  year INTEGER,
  rating REAL,
  genre TEXT,
  director TEXT,
  description TEXT,
  image TEXT,
  tmdb_id INTEGER,
  trailer_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Створення таблиці favorites
CREATE TABLE IF NOT EXISTS public.favorites (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE,
  movie_id UUID NOT NULL REFERENCES public.movies(id) ON DELETE CASCADE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(user_id, movie_id)
);

-- Створення індексів для швидшого пошуку
CREATE INDEX IF NOT EXISTS idx_favorites_user_id ON public.favorites(user_id);
CREATE INDEX IF NOT EXISTS idx_favorites_movie_id ON public.favorites(movie_id);
CREATE INDEX IF NOT EXISTS idx_movies_tmdb_id ON public.movies(tmdb_id);

-- Увімкнення Row Level Security (RLS)
ALTER TABLE public.users ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.movies ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.favorites ENABLE ROW LEVEL SECURITY;

-- Політики безпеки для таблиці users
CREATE POLICY "Users can view all users" ON public.users
  FOR SELECT USING (true);

CREATE POLICY "Users can insert their own data" ON public.users
  FOR INSERT WITH CHECK (auth.uid() = id);

CREATE POLICY "Users can update their own data" ON public.users
  FOR UPDATE USING (auth.uid() = id);

-- Політики безпеки для таблиці movies
CREATE POLICY "Movies are viewable by everyone" ON public.movies
  FOR SELECT USING (true);

CREATE POLICY "Authenticated users can insert movies" ON public.movies
  FOR INSERT WITH CHECK (auth.role() = 'authenticated');

CREATE POLICY "Authenticated users can update movies" ON public.movies
  FOR UPDATE USING (auth.role() = 'authenticated');

CREATE POLICY "Authenticated users can delete movies" ON public.movies
  FOR DELETE USING (auth.role() = 'authenticated');

-- Політики безпеки для таблиці favorites
CREATE POLICY "Users can view their own favorites" ON public.favorites
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert their own favorites" ON public.favorites
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can delete their own favorites" ON public.favorites
  FOR DELETE USING (auth.uid() = user_id);

-- Функція для автоматичного створення запису в users при реєстрації
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.users (id, email, role)
  VALUES (
    NEW.id,
    NEW.email,
    CASE WHEN NEW.email = 'gghub491@gmail.com' THEN 'admin' ELSE 'user' END
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Тригер для автоматичного виклику функції при реєстрації
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- Додавання адміністратора для існуючого користувача (якщо потрібно)
-- Замініть 'gghub491@gmail.com' на ваш email, якщо потрібно
UPDATE public.users SET role = 'admin' WHERE email = 'gghub491@gmail.com';
```

### Що робить цей SQL код:

1. **Створює таблицю `users`** - зберігає додаткову інформацію про користувачів (email, role)
2. **Створює таблицю `movies`** - зберігає інформацію про фільми
3. **Створює таблицю `favorites`** - зберігає улюблені фільми користувачів
4. **Створює індекси** - прискорює пошук даних
5. **Налаштовує RLS** - забезпечує безпеку даних
6. **Створює політики безпеки** - визначає, хто може читати/писати дані
7. **Створює функцію та тригер** - автоматично створює запис у таблиці users при реєстрації

## Крок 5: Налаштування автентифікації

1. У вашому проекті Supabase в лівому меню натисніть **"Authentication"** (🔐)
2. Перейдіть у вкладку **"Providers"**
3. Переконайтеся, що **"Email"** провайдер увімкнений (він увімкнений за замовчуванням)
4. Опціонально: налаштуйте **"Email templates"** для підтвердження email (вкладка "Email Templates")

### Вимкнення підтвердження email (для тестування):

⚠️ **ВАЖЛИВО:** Якщо після реєстрації не можете увійти, потрібно вимкнути підтвердження email:

1. У вашому проекті Supabase в лівому меню натисніть **"Authentication"** (🔐)
2. Перейдіть у вкладку **"Providers"**
3. Знайдіть **"Email"** провайдер та натисніть на нього
4. **Вимкніть** опцію **"Confirm email"** (зніміть галочку)
5. Натисніть **"Save"**

Після цього користувачі зможуть входити одразу після реєстрації без підтвердження email.

⚠️ **Увага:** Для продакшену краще залишити підтвердження email увімкненим!

## Крок 6: Встановлення залежностей

Відкрийте термінал у папці проєкту та виконайте:

```bash
cd FilmHub
npm install
```

## Крок 7: Перевірка роботи

1. Запустіть додаток:
   ```bash
   npm start
   ```

2. Відкрийте веб-версію (натисніть `w` в терміналі або відкрийте http://localhost:19006)

3. Спробуйте зареєструвати нового користувача:
   - Введіть email та пароль
   - Натисніть "Registrovat se"
   - Якщо підтвердження email вимкнено, ви одразу увійдете
   - Якщо підтвердження email увімкнено, перевірте пошту та підтвердіть email

4. Перевірте в Supabase:
   - Перейдіть у **"Table Editor"** → **"users"**
   - Ви повинні побачити нового користувача з вашим email

5. Спробуйте увійти з мобільного пристрою тим самим email/паролем

## Важливо!

- ✅ Після налаштування Supabase всі дані будуть синхронізуватися між мобільною та веб-версією
- ✅ Якщо Supabase не налаштовано, додаток продовжить працювати з локальним сховищем (SQLite для мобільних, localStorage для веб)
- ⚠️ Дані з локального сховища НЕ будуть автоматично перенесені в Supabase - вам потрібно буде зареєструватися заново

## Вирішення проблем

### Помилка "Invalid API key"
- Перевірте, чи правильно скопійовані `SUPABASE_URL` та `SUPABASE_PUBLIC_KEY` у файлі `supabase.js`
- Переконайтеся, що ви використовуєте **Publishable** ключ, а не Secret

### Помилка "relation does not exist"
- Переконайтеся, що ви виконали всі SQL запити з Кроку 4
- Перевірте, чи всі таблиці створені в **Table Editor**

### Помилка "new row violates row-level security policy"
- Переконайтеся, що ви виконали всі CREATE POLICY запити з SQL коду
- Перевірте, чи RLS увімкнено для всіх таблиць

### Дані не синхронізуються
- Перевірте консоль браузера/терміналу на наявність помилок
- Переконайтеся, що користувач увійшов у систему (перевірте в **Authentication** → **Users**)

### Користувач не може зареєструватися
- Перевірте, чи функція `handle_new_user()` створена правильно
- Перевірте, чи тригер `on_auth_user_created` створений
- Перевірте логи в **Database** → **Logs**

## Додаткові налаштування

### Перевірка створених таблиць:

1. Перейдіть у **"Table Editor"** в лівому меню
2. Ви повинні побачити три таблиці: `users`, `movies`, `favorites`

### Перевірка політик безпеки:

1. Перейдіть у **"Authentication"** → **"Policies"**
2. Або в **"Table Editor"** натисніть на таблицю → вкладка **"Policies"**
3. Ви повинні побачити всі створені політики

### Перевірка функцій та тригерів:

1. Перейдіть у **"Database"** → **"Functions"**
2. Ви повинні побачити функцію `handle_new_user`
3. Перейдіть у **"Database"** → **"Triggers"**
4. Ви повинні побачити тригер `on_auth_user_created`
